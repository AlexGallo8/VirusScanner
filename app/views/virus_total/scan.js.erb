// Remove loading screen
document.getElementById('loadingScreen').remove();

// Redirect to results page or update the page with results
window.location.href = '<%= @results_path %>';  // Replace with your actual results path